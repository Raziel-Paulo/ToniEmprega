@* Views/Account/Login.cshtml - MODIFICADO *@
@{
    ViewData["Title"] = "Login";
}

<div style="max-width: 450px; margin: 2rem auto;">
    <div class="card">
        <div class="card-header" style="text-align: center;">
            <h2 class="card-title"><i class="fas fa-sign-in-alt"></i> Iniciar Sessão</h2>
        </div>

        @if (ViewBag.Error != null)
        {
            <div class="alert alert-error"><i class="fas fa-exclamation-circle"></i> @ViewBag.Error</div>
        }

        <form method="post">
            <div class="form-group">
                <label class="form-label"><i class="fas fa-envelope"></i> Email</label>
                <input type="email" name="email" class="form-control" required />
            </div>
            <div class="form-group">
                <label class="form-label"><i class="fas fa-lock"></i> Palavra-passe</label>
                <input type="password" name="password" class="form-control" required />
            </div>
            <button type="submit" class="btn btn-primary" style="width: 100%;">
                <i class="fas fa-sign-in-alt"></i> Entrar
            </button>
        </form>

        <!-- ✅ NOVO: Link para recuperar password -->
        <div style="text-align: center; margin-top: 1rem;">
            <a href="/Account/RecuperarPassword" style="color: var(--color-blue); font-size: 0.9rem;">
                <i class="fas fa-key"></i> Esqueceu a password?
            </a>
        </div>

        <div style="text-align: center; margin-top: 1.5rem;">
            <a href="/Account/Register" class="btn btn-outline" style="width: 100%;">Criar Conta</a>
        </div>
    </div>
</div>